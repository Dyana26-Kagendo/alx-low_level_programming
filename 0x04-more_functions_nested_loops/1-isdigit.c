#include "main.h"
/*
 * _isdigit - check if character is a digit (0 through 9)
 * character
 * Return (1) if c is character, (0) otherwis
 *
 */
int _isdigit(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}

